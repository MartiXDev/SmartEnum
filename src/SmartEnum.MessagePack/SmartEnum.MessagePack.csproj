<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<PackageId>MartiX.SmartEnum.MessagePack</PackageId>
		<Title>MartiX.SmartEnum.MessagePack</Title>
		<Description>MessagePack (de)serialization support for MartiX.SmartEnum.</Description>
		<Summary>MessagePack (de)serialization support for MartiX.SmartEnum.</Summary>
		<PackageTags>enum;smartenum;ardalis;messagepack;resolver</PackageTags>
		<PackageReleaseNotes>See root package release notes</PackageReleaseNotes>
		<PackageIcon>icon.png</PackageIcon>
		<AssemblyName>MartiX.SmartEnum.MessagePack</AssemblyName>
		<RootNamespace>MartiX.SmartEnum.MessagePack</RootNamespace>
	</PropertyGroup>
	<PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
		<DocumentationFile>bin\$(Configuration)\$(TargetFramework)\$(AssemblyName).xml</DocumentationFile>
	</PropertyGroup>
	<ItemGroup>
		<PackageReference Include="MessagePack" />
	</ItemGroup>
	<ItemGroup>
		<None Include="$(SolutionDir)img\icon.png" Pack="true" Visible="false" PackagePath="" />
	</ItemGroup>
	<ItemGroup>
		<ProjectReference Include="..\SmartEnum\SmartEnum.csproj" />
	</ItemGroup>
</Project>